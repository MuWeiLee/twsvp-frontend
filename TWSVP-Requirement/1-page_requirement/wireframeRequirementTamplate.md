# 页面设计文档

## 0. 基本信息
- 页面名称：
- 页面英文名（Route / Key）：
- 页面层级：（#1 主导航 / #2 二级聚合 / #3 表单 / 弹窗）
- 访问前提：（未登录 / 已登录 / 首次登录）
- 入口来源：（哪些页面 / 哪些按钮可进入）
- 页面出口：（返回到哪里 / 跳转规则）
- 是否在底部 Tab 中：（是 / 否）
- 对应核心数据对象：（如 Feed / User / Stock）

---

## 1. 页面目标（Goal）
> 用 1–3 条话说明「**这个页面存在是为了解决什么问题**」

- 目标 1：
- 目标 2：
- （可选）非目标（刻意不做的事）：

---

## 2. 页面角色与权限（Roles & Permission）
- 可访问用户：
  - [ ] 未登录
  - [ ] 已登录
  - [ ] 本人视角
  - [ ] 他人视角
- 特殊权限说明：
  - 谁可以编辑 / 删除 / 操作哪些内容

---

## 3. 页面整体结构（Layout）
> 只描述**结构层级，不写视觉细节**

### 3.1 顶部栏（Header）
- 左侧：
- 中间：
- 右侧：
- 行为说明：（点击后发生什么）

### 3.2 置顶内容层（Pinned / Sticky Area）
- 是否存在：是 / 否
- 包含模块：
  - 模块 A：
  - 模块 B：
- 作用说明：

### 3.3 内容层（Content Area）
- 内容类型：（列表 / 表单 / 卡片 / 混合）
- 包含模块：
  - 模块 1：
  - 模块 2：
- 默认排序 / 展示规则：

### 3.4 底部操作区（Bottom Action Area）
- 是否存在：是 / 否
- 按钮 / 操作：
  - 主操作：
  - 次操作：

### 3.5 底部 Tab（Bottom Tab）
- 是否存在：是 / 否
- Tab 项：
  - 
  - 
- 当前高亮规则：

---

## 4. 页面模块说明（Modules）
> 对每一个**业务模块**单独说明（可重复）

### 4.x 模块名称
- 模块目标：
- 模块类型：（列表 / 卡片 / 输入 / 统计）
- 包含字段：
  - 字段 1：
  - 字段 2：
- 主要交互：
  - 交互 1：
  - 交互 2：
- 状态：
  - 空态：
  - Loading：
  - Error：

---

## 5. 核心字段与数据结构（Data）
> 只写「**页面真正依赖的字段**」，不是全表字段

### 5.1 核心字段
| 字段名 | 类型 | 来源 | 是否必需 | 说明 |
|---|---|---|---|---|
|  |  |  |  |  |

### 5.2 枚举值（如有）
```txt
enum:
- 
- 